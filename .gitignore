# OpenTofu / Terraform
tofu/.terraform/
# .terraform.lock.hcl is intentionally NOT ignored — committing it ensures
# all contributors get identical provider versions on tofu init
tofu/terraform.tfstate
tofu/terraform.tfstate.backup
tofu/*.tf~
